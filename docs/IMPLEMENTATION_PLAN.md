# 解压小游戏合集：第一阶段实施步骤

> 状态：已确认，等待逐步执行  
> 上位文档：[ARCHITECTURE.md](./ARCHITECTURE.md)  
> 最后更新：2026-08-01

## 1. 本计划的范围

本计划是整体产品计划的第一部分，目标是完成：

- 可持续扩展的架构底座。
- 大厅到小游戏再返回大厅的正式运行链路。
- 第一个完整小游戏“泡泡解压”。
- 第二个极简 3D 小游戏对 2D/3D 架构扩展性的验证。
- 微信分包和真机运行闭环。

本计划暂不包含：

- 大批量生产和上线更多玩法。
- 正式美术资源的规模化制作与完整品牌包装。
- 正式广告位、商业化参数及收入优化。
- 正式数据平台、运营后台和远程内容管理系统。
- 活动、任务、成就、排行榜、分享裂变等运营系统。
- 全量兼容测试、提审、发布和上线后的增长迭代。

以上内容将在本计划通过质量门禁后，拆分为后续独立计划。本阶段建立接口和扩展点，但不会用临时实现破坏未来边界。

## 2. 执行规则

1. 严格按照编号顺序执行，除非先更新本计划并说明原因。
2. 每次只执行一个步骤，不顺带实现下一步。
3. 每一步完成后先验证，再将复选框改为已完成。
4. 验证失败时只修复当前步骤，不通过堆叠后续功能掩盖问题。
5. 修改核心协议、状态机、Bundle 边界或存档结构前，先更新架构文档。
6. 每一步应保持 Cocos Creator 可打开，TypeScript 可编译。
7. 不覆盖与当前步骤无关的用户改动。
8. 在满足功能的前提下选择最简单实现，不为未确认的需求提前增加抽象。
9. 每一步完成后必须 Review 当步改动及其与既有代码的交互，再决定是否勾选完成。
10. Review 至少检查：职责是否单一、依赖方向、重复代码、命名、异常路径、监听/计时器/资源释放、类型安全和测试覆盖。
11. Review 发现的问题在当步修复；涉及架构变更时先更新架构文档，不把已知债务推给后续步骤。
12. 每一步的 Review 结论记录在 [REVIEW_LOG.md](./REVIEW_LOG.md)。

## 3. A：建立工程边界

- [x] **第 1 步：创建目录骨架**
  - 只创建架构文档规定的目录和必要占位文件，不实现逻辑。
  - 验收：目录职责清楚；无循环依赖；Cocos Creator 可以正常打开项目。

- [x] **第 2 步：定义公共基础类型**
  - 定义 `Unsubscribe`、`SafeArea`、`LaunchOptions`、`DeviceProfile`、`GameResult`、`GameServices`。
  - 验收：TypeScript 编译通过；类型不依赖场景或具体小游戏。

- [x] **第 3 步：定义 MiniGame 协议**
  - 只定义 `MiniGame` 和 `MiniGameContext` 生命周期接口。
  - 验收：协议与架构基线一致；不引用具体游戏。

- [x] **第 4 步：定义游戏清单结构**
  - 只定义 `GameManifest`，暂不加载配置。
  - 验收：可以表达 Bundle、场景、入口、版本、展示信息、2D/3D 模式、最低设备档位和启用状态。

- [x] **第 5 步：定义应用状态**
  - 只定义 `AppState` 和合法转换表。
  - 验收：合法转换完整；非法转换可以被识别。

## 4. B：实现最小核心设施

- [x] **第 6 步：实现事件总线**
  - 只实现订阅、取消订阅、发布和清空。
  - 验收：支持多个监听者；取消和清空后不残留引用。

- [x] **第 7 步：实现服务容器**
  - 只实现注册、获取、存在检查和重复注册保护。
  - 验收：缺失服务给出明确错误；业务无需自行创建全局服务。

- [x] **第 8 步：实现状态机本体**
  - 只实现状态转换，不处理场景。
  - 验收：合法转换成功；非法转换被拒绝；转换事件正确；重复转换无副作用。

- [x] **第 9 步：定义平台接口**
  - 只创建 `Platform` 接口，不写平台实现。
  - 验收：业务所需能力均有抽象；接口不暴露 `wx` 类型。

- [x] **第 10 步：实现浏览器平台适配**
  - 只实现安全区、启动参数、可注入设备档位、前后台模拟、振动和分享的安全空实现。
  - 验收：Creator 浏览器预览不依赖微信环境；所有方法均可安全调用。

- [x] **第 11 步：实现微信平台适配**
  - 只封装微信平台基础能力与设备档位，不接广告。
  - 验收：`wx.*` 仅存在于平台层；设备信息被标准化；前后台监听可解除；非微信环境不会误执行。

## 5. C：建立应用启动流程

- [x] **第 12 步：创建 App.scene**
  - 只创建 Canvas、PersistentRoot、ServiceRoot、AudioRoot、LoadingLayer 和 SceneContainer。
  - 验收：场景可预览；层级符合架构基线；不包含大厅或游戏业务。

- [x] **第 13 步：实现 App 启动组件**
  - 只负责持久节点、平台判断、服务容器创建和进入 `booting`。
  - 验收：初始化只执行一次；场景切换后常驻节点仍存在。

- [x] **第 14 步：接入前后台生命周期**
  - 只连接平台 `onHide`、`onShow` 与暂停恢复请求。
  - 验收：不会重复暂停或恢复；非游戏状态不会错误进入 `playing`。

- [x] **第 15 步：增加启动错误处理**
  - 只为启动流程增加统一错误捕获。
  - 验收：失败时进入 `error`；错误包含失败阶段；不出现无响应空白画面。

## 6. D：配置和游戏注册

- [x] **第 16 步：创建本地应用配置**
  - 只创建应用版本、默认语言、设计分辨率、渲染层、3D 画质档位、超时和开发开关。
  - 验收：配置格式可校验；字段缺失时有默认值。

- [x] **第 17 步：实现 ConfigService 本地读取**
  - 暂不获取远程配置。
  - 验收：可以读取本地配置；失败时回退默认值；不暴露原始 JSON。

- [x] **第 18 步：创建 games.json**
  - 只登记一个占位游戏。
  - 验收：符合 `GameManifest`；ID、Bundle、场景和展示信息完整。

- [x] **第 19 步：实现 Manifest 校验器**
  - 只校验必填项、格式、重复 ID、版本、2D/3D 模式、最低设备档位和资源路径。
  - 验收：错误信息包含字段和原因；单条错误不会导致校验器崩溃。

- [x] **第 20 步：实现 GameRegistry**
  - 只实现载入、保存、按 ID 查询和启用列表查询。
  - 验收：重复注册被拒绝；禁用或设备档位不兼容的游戏不能作为可玩游戏返回。

- [x] **第 20.1 步：接入本地游戏目录启动加载**
  - 只让配置层读取 `games.json`，完成校验后将清单载入 `GameRegistry`，并把 Registry 注册到服务容器。
  - 验收：启动后 Registry 包含一个占位游戏；目录读取或校验失败时进入明确的启动错误阶段；不由 Registry 直接读取 Cocos 资源。

## 7. E：建立空大厅

- [x] **第 21 步：创建大厅 Bundle**
  - 只配置 `lobby` Asset Bundle。
  - 验收：Bundle 可独立加载；不包含小游戏资源。

- [x] **第 22 步：创建空 Lobby.scene**
  - 只创建背景和公共 UI 层级。
  - 验收：`lobby` Bundle 可按名称加载；大厅可显示；安全区正确；暂不显示游戏卡片。

- [x] **第 23 步：实现大厅入口组件**
  - 只从 `GameRegistry` 获取游戏列表。
  - 验收：游戏数量正确；不引用具体小游戏类。

- [x] **第 24 步：制作游戏卡片 Prefab**
  - 只包含图标、名称、简介、状态和点击区域。
  - 验收：卡片可单独预览；不包含加载游戏逻辑。

- [x] **第 25 步：根据配置生成卡片**
  - 只将 Manifest 渲染成列表。
  - 验收：数量、排序和禁用状态正确；修改配置无需修改大厅代码。

- [x] **第 26 步：处理大厅重复点击**
  - 只增加进入请求点击锁。
  - 验收：快速点击只产生一次请求；成功或失败后正确解锁。

## 8. F：建立游戏运行链路

- [ ] **第 27 步：实现 GameSession**
  - 只管理 Session ID、游戏 ID、开始时间、状态和结果。
  - 验收：每次进入 ID 不同；不能重复结束；时长计算正确。

- [ ] **第 28 步：实现 Asset Bundle 加载**
  - `AssetService` 此时只实现 Bundle 加载。
  - 验收：并发加载合并；失败错误明确；不重复创建加载任务。

- [ ] **第 29 步：创建占位游戏 Bundle**
  - 只创建独立 Bundle、场景、入口组件和退出按钮。
  - 验收：不依赖其他游戏；场景可以独立加载。

- [ ] **第 30 步：让占位游戏实现 MiniGame**
  - 实现全部生命周期方法，但不增加真实玩法。
  - 验收：调用顺序可观察；错误顺序可以被发现。

- [ ] **第 31 步：实现游戏入口定位**
  - `GameLoader` 只负责寻找并校验入口组件。
  - 验收：正常入口可定位；缺失或协议不符时返回明确错误。

- [ ] **第 32 步：实现进入游戏流程**
  - 只打通创建 Session、加载 Bundle/场景、初始化和开始。
  - 验收：状态顺序正确；快速点击不创建多个 Session；失败时不进入 `playing`。

- [ ] **第 33 步：实现退出游戏流程**
  - 只打通请求退出、暂停、销毁和返回大厅。
  - 验收：游戏不直接切场景；多次退出只执行一次；最终回到 `lobby`。

- [ ] **第 34 步：实现 Bundle 释放**
  - 只在退出完成后释放当前游戏 Bundle。
  - 验收：游戏 Bundle 不驻留；公共资源不被误释放；可以再次进入。

- [ ] **第 35 步：实现统一加载界面**
  - 只处理显示、隐藏、文案和触摸拦截。
  - 验收：加载期间不能操作大厅；成功或失败都会关闭。

- [ ] **第 36 步：实现统一错误页**
  - 只处理游戏加载错误。
  - 验收：可以重试或返回大厅；重试不复用已损坏的 Session。

## 9. G：完成公共能力

- [ ] **第 37 步：实现版本化存档**
  - 只实现读取、写入、默认值和 `schemaVersion`。
  - 验收：首次启动生成默认数据；损坏数据可恢复；游戏数据互不覆盖。

- [ ] **第 38 步：增加存档迁移器**
  - 只实现一个示例迁移流程。
  - 验收：旧数据可逐级升级；失败时保留可恢复信息。

- [ ] **第 39 步：实现音频服务**
  - 只支持背景音乐、单次音效、总开关和前后台暂停恢复。
  - 验收：背景音乐不重复；设置可保存；返回前台不叠加播放。

- [ ] **第 40 步：实现统计服务**
  - 开发环境只输出结构化日志。
  - 验收：自动补充公共字段；同一次结束不重复上报；业务不依赖统计厂商。

- [ ] **第 41 步：实现广告服务接口**
  - 只实现可配置结果的模拟广告。
  - 验收：可模拟看完、跳过和失败；广告期间暂停；结束后恢复正确状态。

- [ ] **第 42 步：实现公共暂停弹窗**
  - 只提供继续、重新开始和退出。
  - 验收：所有操作通过 Session 和状态机，不直接控制具体游戏节点。

- [ ] **第 43 步：实现公共结果弹窗**
  - 只展示标准 `GameResult`，提供重开与返回大厅。
  - 验收：不依赖具体小游戏数据结构。

## 10. H：实现第一个正式小游戏

- [ ] **第 44 步：创建泡泡游戏 Bundle**
  - 只创建目录、Bundle 和场景。
  - 验收：资源全部位于自身 Bundle；不引用占位游戏。

- [ ] **第 45 步：搭建泡泡游戏静态界面**
  - 只做视觉布局，不做点击逻辑。
  - 验收：安全区和常见屏幕比例适配正确。

- [ ] **第 46 步：实现泡泡生成**
  - 只根据配置生成泡泡。
  - 验收：数量和位置正确；重开结果可控；不重叠或越界。

- [ ] **第 47 步：实现单个泡泡点击**
  - 只处理命中、消失动画和重复点击保护。
  - 验收：单个泡泡只结算一次；动画后节点销毁或回收。

- [ ] **第 48 步：实现游戏进度**
  - 只统计并展示剩余数量。
  - 验收：点击后数量正确；重开后完全重置。

- [ ] **第 49 步：实现游戏完成**
  - 剩余数量为零时生成标准 `GameResult`。
  - 验收：只结算一次；公共结果弹窗可以正常展示。

- [ ] **第 50 步：接入音效和振动**
  - 只通过公共服务接入反馈。
  - 验收：设置立即生效；浏览器环境安全运行；游戏内无直接平台调用。

- [ ] **第 51 步：接入存档和统计**
  - 只记录游玩/完成次数、最佳时长及开始、完成、退出事件。
  - 验收：中途退出和正常完成可区分；不重复存储或上报。

## 11. I：验证架构成立

- [ ] **第 52 步：执行连续进出压力测试**
  - 连续进入、退出泡泡游戏 30 次。
  - 验收：无持续内存增长、重复事件、残留音频或多个 Session。

- [ ] **第 53 步：创建极简 3D 验证游戏**
  - 使用“3D 推倒积木”验证 3D Camera、简单光照、物理、Shader 预热和三档画质，只实现最基本玩法。
  - 验收：不修改大厅、`GameLoader` 和生命周期协议；只新增 3D 游戏模块、资源和 Manifest。

- [ ] **第 54 步：验证游戏隔离**
  - 交替进入 2D 泡泡游戏和 3D 推倒积木游戏。
  - 验收：Camera、Layer、物理、Shader、CPU/GPU 资源、存档和分数不串用；两个 Bundle 可以分别释放。

- [ ] **第 55 步：配置微信分包**
  - 只将两个小游戏映射到不同分包并检查产物。
  - 验收：主包不含游戏专属资源；分包路径正确；微信开发者工具可加载。

- [ ] **第 56 步：执行微信真机验证**
  - 检查冷启动、安全区、分包、弱网、前后台、音效、振动、2D/3D 画质路径、CPU/GPU 内存和错误恢复。
  - 验收：任一失败都能恢复到可操作状态，不需要重启小游戏。

## 12. 本计划完成条件

只有同时满足以下条件，本计划才能标记为完成：

- 56 个步骤均完成并通过各自验收。
- 架构基线中的首期质量门禁全部通过。
- 第二个小游戏接入没有修改大厅或核心加载流程。
- 极简 3D 游戏完成三档画质与退出资源释放验证。
- 微信真机完成从启动到两个游戏再返回大厅的闭环。
- 已记录测试结果、剩余风险和后续计划输入。

## 13. 后续计划入口

完成本计划后，再根据实际验证结果分别建立：

1. 游戏内容生产计划。
2. 视觉与动效系统计划。
3. 商业化与广告接入计划。
4. 数据分析与运营系统计划。
5. 测试、提审与发布计划。
6. 上线后监控、留存和增长迭代计划。

这些计划不得反向破坏本阶段确定的平台边界、小游戏协议、资源隔离和生命周期管理。
