# 深海微光首轮视觉评审

状态：用户最新选择改回章鱼 Boss v7，当前唯一造型制作依据为 v7。此前 v8 及更早版本的评审图和生成记录已清理；深海回合逻辑已验证，正式场景尚未接入。前置暂停弹窗已获用户确认，最终面板宽 580、按钮宽 485，高度 110/92、间距 24。

## 本轮提案

- 场景：蓝绿透光海水，远景沉没遗迹，少量水母，珊瑚/海草集中在边缘；中心保留瞄准空间。
- 泡泡：透明气泡膜包裹彩色珠核，红心、黄星、蓝水滴、紫葡萄保持一致，碰撞依旧严格正圆。
- 普通机制：局部海草环与小锚点标记表示额外支撑；不使用贯穿棋盘的长海草。
- Boss：粉紫色章鱼、大头与吸盘腕足，替换云端布丁王；抓取腕足置于泡泡后方，目标仍可正常匹配。
- 发射器：珍珠金属与浅色底座，仍拆成固定底座/旋转炮台；当前球、下一球、交换和三个道具沿用操作位置。

## 生成偏差与制作约束

这张图是视觉提案，不作为坐标或规则依据。普通图顶排列数未准确遵守 10/9；正式棋盘仍由六角模型生成。海草锚点图标不能替代红心等原纹样，制作时放在环边的小角标处。Boss 图缺暂停入口，正式 HUD 必须保留。抓取泡泡不能脱离合法格位悬在棋盘外。道具中的清底仍带糖果参考特征，海洋道具需单独细化，不能直接作为确认素材。背景细节密度、HUD、发射器和道具均在后续细化阶段检查。

先确认整体环境、珠核材质和章鱼造型，再细化有偏差的 HUD / 道具 / 锚点组合。新增素材仍按效果图确认、拆分、实现的顺序推进。奖励、暂停、复活和结算继续使用已有通用主体；海洋 Boss 叠顶组合需另出效果图确认，不重绘通用底板。

## 章鱼 v7：适中细节的风格化 Boss

用户反馈 v6 过于卡通、过于简化，造型仍不满意。修正方向为中等细节的风格化奇幻游戏角色：缩小眼睛，腕足更修长，保留装备层次与材质区分；取消小海螺帽，改成低矮宽阔的海底遗迹冠冕，与披肩和护腕统一。见 [v7](references/ocean-octopus-lord-review-v7.png)。用户在比较 v8 后重新选择 v7，现作为最终造型依据。实际内置 imagegen 提示词见 art-source/ocean-octopus-v7-prompt.md。

制作冻结：严格沿用 v7 的紫色皮肤、青金鳍刃冠冕、分层披肩、护腕、珍珠权杖，以及 v7 原图的眉眼、闭口神情与腕足姿态；不再沿用 v8 的挑眉笑容，也不自行混合两版表情。按身体、冠冕、持杖腕足、抬起腕足与抓取腕足拆层，服务待机、入场、施法和退场动效。透明与尺寸处理沿用用户已授权的本地流程。通用弹窗主体保持已有资源，海洋 Boss 叠顶组合另出效果图确认。正式棋盘沿用当前 15/14 列和既有顶部位置，早期提案中的 10/9 列不再适用。

## v8 首批拆分（已停用）

内置 imagegen 曾生成 v8 身体与冠冕；相关运行时导出、评审图和提示词已清理。v7 分层由 `art-source/ocean/v7/` 和 `runtime-source/ocean/v7/` 管理，当前只保留 v7 的制作资料。

- v8 身体与冠冕运行时文件：已清理，不进入正式资源引用。
- v7 的身体、冠冕、持杖腕足、抬起腕足和抓取腕足：见下方 v7 分层记录。
- 海洋暂停叠顶使用 `art-source/ocean-pause-v2-prompt.md` 和 v7 参考图，当前效果图与层级方式已确认；旧版生成记录已清理。

## 深海回合逻辑与存档（已验证，尚未开放正式入口）

`BubbleShooterRound.reset('ocean')` 使用现有 15/14 列布局，普通开局放置 4 个分散的海草支撑点；连续累计 3 次未消除仍压入一行，并在新行增加海草支撑，场上支撑总数上限为 6。支撑不改变珠子的颜色和正常匹配资格。普通进度、队列、道具、危险判断沿用现有规则。

章鱼阶段重新生成棋盘，清除普通海草，不产生云端冰冻。每 3 次发射后压入一行，再抓住最多 2 个仍存在且尚未被抓取的珠子，总支撑上限为 6。抓取与珠子格位绑定，插行时一起下移；匹配或道具移除抓取点后，模型重新检查顶部和所有剩余支撑点的连通性，掉落计入伤害。致胜伤害优先于插行与抓取。

深海快照使用 v4，明确保存 region 与珠子 support；旧云端 v1/v2/v3 继续兼容。恢复在验证完成前不修改当前回合，拒绝错误场景、混用冰冻、错误支撑种类、超量支撑和非法目标。`continueRegion()` 保留道具、待发队列和整局复活使用状态。正式视图当前仍仅允许云端存档，避免在云端画面中恢复不可见的深海支撑；后续素材接入时再放开可用区域。

已通过 `verify-ocean-round.cjs`、`verify-ocean-support.cjs`、`verify-cloud-complete.cjs`、`verify-round.cjs` 与 `verify-items.cjs`。尚未完成：深海 SpriteFrame/动画装配、海洋弹窗叠顶评审、当前区域资源加载和随机不重复区域轮换。不能将逻辑层验证当作第二场景已完成。

## v7 分层制作与海洋弹窗叠顶确认

用户已确认 `references/ocean-pause-boss-v7-review.png` 的叠顶方式：v7 Boss 的头部和披肩在通用面板后方，腕足搭在面板上沿，脸、标题和按钮完整露出。后续海洋奖励、暂停、复活和结算沿用此层级原则；面板主体和按钮直接使用已有通用资源。效果图中的背景仅用于展示组合，实际布局仍遵守顶部胶囊、底部安全区和已确认的面板尺寸。

本批内置 imagegen 按 v7 重新生成五个分层，母图及实际提示词位于 `art-source/ocean/v7/`，透明裁切和缩放产物在 `runtime-source/ocean/v7/`，已复制到正式资源目录 `assets/game-assets/bubble-shooter/visual/regions/ocean/boss/`，尚未添加场景引用。v8 历史素材不再参与装配。

- boss-body：768 × 753，身体、披肩和下部静态腕足。
- boss-crown：384 × 258，独立冠冕，可单独摆动。
- boss-staff-arm：290 × 768，持杖腕足与权杖一起运动。
- boss-right-arm：384 × 223，右侧动作腕足，根部藏在身体后方。
- boss-grab-arm：259 × 512，抓取腕足，置于棋盘珠子后方，图中不烘焙珠子。

脚本 `tools/bubble-shooter/prepare-ocean-v7.py` 重现抠图、封闭孔洞背景清理、alpha 杂点清除及等比缩放；源图不改造型，保留真实 alpha 时直接沿用。`layers.json` 记录裁切范围与输出尺寸。各层装配、动作以及深海游戏画面接入仍待实现，此批完成不代表第二场景已可玩。

## v7 Cocos 装配与动作控制器

新增 `BubbleShooterOceanBoss.ts`：通过 AssetService 仅准备 `visual/regions/ocean/boss`，再读取已加载的五个 SpriteFrame；不直接下载或卸载 Bundle。组件宿主必须控制 `paused`，或禁用组件自动 update 并由游戏统一驱动 update，二者不能同时推进。

- 五层装配已在 Cocos 实际渲染检查，修正冠冕与腕足过高的锚点。
- 入场 0.9 秒浮起淡入；待机呼吸、冠冕和腕足错拍轻摆；施法 1 秒挥杖抬腕，0.42 秒触发一次 impact 回调；退场 1.15 秒晃动下沉淡出。
- 重复入场、施法和退场锁定；暂停不推进时钟；dispose 清除回调、节点、抓取记录及 SpriteFrame 引用，重复 dispose 安全。
- syncGrabs 与模型提交同步；插行时传入 inserted=true，使已有抓取节点随格位移动而不重建；支撑被移除后腕足撤回淡出。
- EndView 和 RewardView 增加 setRegionDecoration 接口，复用原通用面板、按钮和动效；v7 头部披肩在后、腕足在前。奖励标题复用通用浅色标题底板，避免深色文字落在披肩上。

临时 Cocos 装配截图和动效视频已清理；浏览器脚本 `verify-ocean-boss-browser.cjs` 验证独立加载、暂停、回调时机与重复锁、插行抓取节点保留、撤回、弹窗挂载、退场和退出清理。

本批完成的是可挂载的 Boss 控制器和弹窗区域接口，已通过测试夹具在 Cocos 运行；正式 BubbleShooterGame 的场景选择、环境/炮台/珠子素材与 Boss 事件调度尚未切至深海。后续需将 round.supported 和 inserted 传给控制器、在 impact 时显示抓取、在 defeat 完成后展示奖励，并连接当前区域加载流程。不能将装配预览视为深海已开放游玩。

## 最新修正：皇冠/腕足连接与统一奖励庆祝图

用户指出皇冠偏位、玩法右臂以及暂停弹窗腕足浮空。皇冠改为身体子节点，按头部坐标定位并跟随呼吸；左右动作腕足根部移入披肩遮挡区。早期曾尝试在前层使用同源右腕足末端裁片（144 × 223），该裁片随后随旧弹窗方案一起清理；当前弹窗只使用既有后层腕足。

用户明确所有场景三选一采用统一结构：中部卡片与下方控件固定，上部展示包含本场景 Boss 的完整庆祝图片。该决定替代此前将 modalFactory 直接用于 RewardView 的方案；暂停/复活/结算仍使用叠顶分层接口。RewardView 删除 setRegionDecoration，改为 setCelebration(SpriteFrame)，完整庆祝图等比放入上方固定区域；云端现有庆祝构图继续保留。

用户已批准 `references/ocean-reward-celebration-review-v1.png` 的“章鱼＋深海凯旋”。内置 imagegen 拆分母图为 `art-source/ocean/reward/celebration-green.png`，提示词同目录。采用已授权本地脚本 `prepare-ocean-celebration.py` 清除非纯色绿底及半透明杂点，输出 `visual/regions/ocean/reward/celebration.png`（1024 × 691 RGBA）。不改中部卡片和底部控件尺寸位置。

最新 `verify-ocean-boss-browser.cjs` 已通过：6 个 Boss 图层独立加载、入场/待机/暂停/施法、抓取插行和撤回、完整庆祝图挂载、通用中下部几何保持一致、退场与 dispose 清理。TypeScript、Bundle 校验和 git diff --check 通过，Cocos 已刷新。

装配测试已完成，临时 Cocos 截图和视频已清理；正式深海环境、炮台、珠子、HUD、区域加载及正式游戏事件调度尚未全部接入。

## 用户最新层级与尺寸修正

暂停等叠顶弹窗改为左腕足整张在面板前方、右腕足整张在面板后方，不再使用末端裁片叠加。Boss 资源加载列表恢复五个原始分层；历史末端裁片不再加载。

三选一庆祝图取消 340 高度限制，直接以 Panel 实际宽度 710 等比显示，底边固定在面板顶边上方 8 个设计单位。图片与容器始终等宽。新增高度纳入整体弹窗安全区适配，空间不足时整组同比缩小；中下部节点原有坐标与尺寸不变。浏览器生命周期、装配、奖励中下部几何检查通过，TypeScript 和 git diff --check 通过。

## 弹窗专用腕足提案（旧方案，已停止）

用户要求暂停弹窗单独设计腕足，曾先看过完整效果图再拆分。该专用腕足评审图已在本次清理中删除；它只用于评审姿态与连接，不改变正式坐标，也不属于运行时装配。

## 最新决定：两侧腕足均在面板下方

用户取消专用腕足修改，要求沿用原素材，只将左手也放到弹窗面板下方。左右腕足现均在 RegionBack 内，面板统一遮挡下部；专用腕足提案停止，不拆分、不接入。此决定替代之前左手在前右手在后的安排，三选一庆祝图不变。

## 暂停弹窗专用左腕足 v2（待确认）

用户重新提出过左手单独出图并盖在弹窗上层，但该提案后来撤回。对应评审图已清理；它只曾用于确认专用左腕足的形状、连接和遮挡关系，通用面板、标题、按钮和 v7 Boss 头部不受影响。

对应提示词和提取方案已随废弃提案清理；当前不再拆分、不替换运行时素材，也不创建弹窗前景左腕足。

用户曾确认过 v2 专用左腕足效果图。由此产生的 `boss-modal-left-arm` 临时素材已清理，未保留为运行时 SpriteFrame；战斗层继续使用原有分层，三选一庆祝图不变。

此前曾完成过一次临时运行时接入，但该方案已撤回并清理；当前弹窗不再创建 `RegionFront` 左腕足，右腕足仍在 `RegionBack`，面板遮住其下部。

最新层级补充：用户明确左腕足应在章鱼主体上方、弹窗面板下方。LeftArm 放在 RegionBack 内 Body 之后，仍整体位于面板后方；右腕足保持主体后方。

最新位置微调：左腕足在原坐标基础上小幅向右、向下调整到 x=-202、y=244；层级保持不变，以此作为当前暂停弹窗装配坐标。

最新决定：暂停弹窗移除左腕足，只保留右腕足；战斗 Boss 的左腕足和三选一庆祝图不变。

最新位置微调：暂停弹窗后方的持杖手向左移动 15 个设计单位，x=-125、y=285；战斗画面中的持杖手不变。

最新尺寸微调：暂停等叠顶弹窗中的 Boss 整体放大 8%，并向下移动 8 个设计单位；通用面板、标题、按钮与战斗画面尺寸不变。

最新位置微调：在上述基础上，暂停弹窗 Boss 再向下移动 8 个设计单位，累计下移 16 个设计单位；放大比例、横向位置和面板控件不变。

最新修正（2026-09-15）：曾尝试修正独立左手提取图中的重复肩甲，但用户随后决定不再需要这只手。相关源图、前景裁切、运行资源和临时装配截图均已清理；正式弹窗不显示独立左手。

最新决定（2026-09-15）：用户撤回暂停弹窗独立左手。弹窗恢复为持杖手、身体、王冠和右腕足四个原有层级，删除 `modal-left-arm` 的加载、前景节点和运行资源；相关临时源图、提示词和评审图已清理。
