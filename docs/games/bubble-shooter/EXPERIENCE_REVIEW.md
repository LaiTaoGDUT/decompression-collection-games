# 2026-09-16 单场景体验调整

状态：十项代码/素材调整已落地，纯逻辑、动效时钟及 Cocos 浏览器检查通过。2026-09-16 用户重启编辑器后完成实际画面复查；海洋后续开发仍暂停。

1. 新压入行的珠子在自身中心以 0.32 秒缩放回弹渐显，轻微错开出生时间。已有棋盘继续下移；新生行抵消父节点的下移偏移，避免从上方滑落。重建/退出清除动画引用。
2. 云端 FixedConnector 隐藏；imagegen 移除炮台头底部连接残留。保留原 UUID，透明清理后仍为 192 × 236。母图、实际提示词位于 art-source/cloud/launcher-clean，处理脚本 prepare-clean-launcher.py。底座继续保留。
3. 云端暂停/复活左右拳头位置由 ±220 调至 ±238。海洋独立装饰不受云端拳头位置影响。
4. 三选一顶部庆祝组件独立从上方落下（0.32 秒），挤压（0.12）、回弹（0.12）、归位（0.16）；白云 Boss＋标题和海洋完整庆祝图共用该动效。中下部不参与额外落下。入场结束前锁定奖励点击。
5. 暂停/复活动效参考 WatermelonOverlayView：横向 .88、纵向 .74 起步，0.24 秒 backOut 展开；0.18 秒收缩淡出，动作回调等待关闭完成。统一游戏时钟推进，取消时正常结束等待。
6. 万能图标缩至 .82，清底顺时针倾斜 45°。复用既有底板裁出独立名称底板 144 × 48，文字区域 132 × 44，23 号字可容纳四字。数量文字白色。保留左侧两行三角布局。
7. 炮台发射/取消瞄准后 .24 秒平滑回正，新的瞄准会中断回正。
8. 装填中心及旋转/发射中心一起上移 6：PIVOT.y=-398，当前球仍处于局部原点，炮台图片相对下移 6，避免图像中心与碰撞轨迹不一致。
9. 下降/技能计数模块按当前实际可见子节点的左右边界整体居中，普通和 Boss 状态分别计算。
10. 棋盘 15/14 → 14/13 列；直径 48 → 720/14，宽度仍720，TOP仍410；警戒线 -330 → -305。普通开局135、Boss开局125，开局行数仍10，安全可容纳行数随放大减少。

## 存档兼容

v5 保存新布局与 region。旧 v1/v2 先走已有迁移，再与 v3/v4 一起按最近合法格位重排进14列网格，保留珠子颜色、冰冻/支撑、道具、进度和复活状态。旧局若珠子超过新安全网格容量，多出的珠子保存在 migrationReserve，随后压行时优先补回，不凭空增加进度，也不因尺寸更新直接使存活局失败；进入 Boss/下一场景时按原有换盘规则清理旧棋盘及队列。

## 验证

通过：verify-model、verify-round、verify-items、verify-cloud-complete、verify-ocean-round、verify-ocean-support、verify-board-migration、verify-layout、verify-removal-motion、verify-experience-motion；TypeScript、Bundle 和 git diff --check。

verify-experience-motion 直接执行生产代码的动效时钟，覆盖展开/收起/取消、庆祝图落下挤压回弹、回正插值、新行中心位置/渐显与引用清理。它不是 Cocos 渲染验收。

实际画面验证脚本：tools/bubble-shooter/verify-experience-browser.cjs，已在重启后的 Cocos 预览中通过，无浏览器 pageerror。验证 14/13 列及 720 宽度、普通/Boss 预告居中、连接柄隐藏、回正插值、道具文字与角标、新行出生状态、暂停展开/收起、庆祝图落下挤压回弹归位与退出清理。复活与暂停共用展开/收起实现；此次录像展示暂停。

实际产物位于 references：experience-board-live.png（棋盘/炮台/道具）、experience-motion-30.png（暂停）、experience-motion-90.png（三选一）、experience-motion-review.mp4（出生、暂停及庆祝动画）。已检查静态截图：名称未溢出，暂停双手向两侧展开，三选一中下部布局保持。浏览器验证不替代后续微信真机验收。

## 2026-09-17 体验二次调整（以此节覆盖上述同项行为）

- 新行出生节点保留棋盘局部坐标，与旧棋盘一起从上边界下移，同时缩放渐显；不再抵消父节点位移。
- 初始、恢复及 Boss/补盘入场逐行逐列错开（行间55ms、列间8ms），最后一颗出生完成后解锁瞄准。
- 云端 Boss 冻结按权重 `1 + row * .35` 无放回随机抽取，越下方单颗珠子概率越高，所有未冻结珠子均可入选。
- BOSS来袭横幅滑入中间后横向挤压、回弹放大、恢复比例，再滑走。
- 结算中按住/移动记录坐标，结算恢复后继续瞄准；松手、取消、暂停和退出清除记录。结算结束本身不清除仍按住的手指。
- 名称底板强制 Sprite.Type.SLICED；按原图73高等比缩放到48高，宽度由九宫格中部延展，左右端保持原始比例。炸弹、万能图标均恢复1倍（80×80）。
- 三选一通用内容从横向82%、纵向68%展开回弹；顶部保持独立落下、挤压、回弹。暂停/复活保留打开效果，关闭直接隐藏，不等待收起动画。
- 本轮按用户要求不运行浏览器、不生成截图。生产动效/触摸状态测试、Round、Cloud complete、TypeScript和git diff --check通过，已刷新Cocos资源。实际观感由用户手动验收；上一节截图仅对应上一轮。

## 2026-09-17 动效力度与复活反馈

- Boss 横幅总时长1.45→2.15秒，稳定停留约1.12秒；中央横向挤压至77%、回弹放大至118%，恢复后再退出。
- 三选一通用面板0.46秒内快速放大至112%、回缩至96%、恢复100%；顶部独立庆祝动画保留。卡片及领取按钮按住缩至94%×92%，释放恢复；取消触摸不执行动作，关闭清理按压状态，未可用控件不响应。
- 炸弹图标改为96×96（此前80×80），补偿图片透明边距造成的视觉偏小；万能球保持80×80。
- 视频复活清除与清底一致的最下面两个有珠子的行，实际消失的珠子复用 animateRemoval 的碎裂/粒子效果，不增加进度或 Boss 伤害。存档仍在复活逻辑提交后保存。
- 清底预览由粗橙圈改为低透明度粉色外光、细奶白亮边及轻微呼吸高光，不改珠子原有造型。
- 未运行浏览器截图。生产动画时钟与按钮按压/取消检查、复活两行/不重复复活/不增收益检查、回合与云端完整逻辑、TypeScript、git diff --check通过；Cocos资源已刷新。实际观感待手动验收。

## 图标画布统一与节奏微调

- 三个道具栏 Icon 节点统一96×96、scale=1；素材均保留192×192完整透明画布，Sprite.trim=false，不让自动裁切透明边距抵消主体大小调整。炸弹沿用现有画布主体，万能球/清底在画布内按160/192缩放。原始图保存在 art-source/cloud/item-icon-sizing，normalize-item-icon-canvas.py 可重复执行，不累积缩小。只做已授权本地尺寸/透明画布处理，不重新绘制造型。
- 清底预览粉色外光不透明度提升到140–195，奶白内圈225–255，外圈6/内圈2.5单位；最低亮度也保持明显。
- Boss横幅整体2.15→1.9秒，稳定停留1.12→0.87秒；保留挤压回弹幅度。
- 三选一通用面板入场0.46→0.66秒，维持先放大再回缩；卡片取消按压缩放，点击选择与选中框保留，领取按钮仍有按压反馈。
- 动效时钟/卡片不缩放/按钮按压测试、TypeScript、git diff --check通过；已刷新资源，不运行浏览器截图。
