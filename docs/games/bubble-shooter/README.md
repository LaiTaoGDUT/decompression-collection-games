# 泡泡龙设计接续入口

最后整理：2026-09-11。来源：用户与助手的逐步设计讨论。

## 当前状态

本游戏处于设计与素材准备阶段。已确认核心循环、四区域机制、道具、复活和云端软糖主界面视觉方向；云端软糖的加高背景已确认，顶部/底部场景层、四色泡泡、糖霜和发射器分层已有原图候选。现有独立物件仍需统一透明、裁切和边缘处理。尚未创建本游戏场景、Bundle、Manifest 注册或业务代码。

`bubble-shooter` 只是本文档目录名，正式游戏名、gameId、Bundle 名尚待确定。

用户最后确认的是：宽体布丁王与较高棋盘布局合并后的最终构图方向。用户随后要求先把全部讨论结果固化到项目，便于任何时候继续。

2026-09-11 用户已确认 [加高天空云海 v2](art-source/cloud/background-tall-v2.png)，要求后续素材干净、减少噪点。内置生图工具一度把透明预览棋盘格写入 RGB 像素；用户随后明确要求先继续生成原图，透明问题统一后处理。顶部/底部场景层、四色泡泡、糖霜覆盖层与碎屑、发射器底座已生成候选。四色泡泡、糖霜、发射器、HUD、道具和宽体布丁王四层现已输出真实透明、合理尺寸的运行时候选；其余带棋盘格的独立物件仍不能用于运行时。v1 背景只留构图参考。详见 [素材进度](ASSET_PROGRESS.md)。

## 阅读顺序

最新接续（2026-09-11）：宽体布丁王的身体、左拳、右拳与皇冠已全部完成真实 Alpha 和运行时尺寸处理；[运行时分层组合检查](art-source/cloud/boss-pudding-runtime-composite-review-v3.png) 保持宽体、大脸、双圆拳与完整皇冠，并验证四层可以独立运动。身体为 1024 × 512，双拳各 384 × 320，皇冠为 320 × 256；原始 1000～1700 像素母版不会直接导入游戏。组合检查只用于素材边界和层级审查，不替代用户已确认的主参考图。发射器继续采用[固定底座](runtime-source/cloud/launcher-pedestal.png)与[可旋转炮台](runtime-source/cloud/launcher-turret.png)分层，当前球随炮台位置旋转并保持纹样正立。四色普通球、糖霜、HUD、攻击反馈和三个道具图标也已完成运行时候选。下一步处理仍有假棋盘格的右上浮岛与底部前景；浮岛是否需要运行时独立缓动，将在导入前按背景重复问题复核。参见 [素材进度](ASSET_PROGRESS.md)。

1. [玩法与流程基线](DESIGN.md)：当前有效规则、四区域、奖励、供球、死亡与复活。
2. [视觉与素材制作基线](VISUAL.md)：适配例外、最终图、素材拆分计划、视觉验收。
3. [待定事项与废弃方案](DECISIONS.md)：后续从哪里继续，哪些旧设计不能恢复。
4. 项目根目录 [AGENTS.md](../../../AGENTS.md) 与 [公共架构](../../common/ARCHITECTURE.md)。

## 已确认效果图

| 文件 | 用途与状态 |
| --- | --- |
| [普通阶段](references/cloud-normal-approved.png) | 普通界面参考；用糖珠槽替换文字补行提示，后续用户要求延续此版制作 Boss |
| [Boss 最终构图](references/cloud-boss-composition-approved.png) | **当前 Boss 视觉主依据**：宽体、大脸、双拳；用户确认整体比例；仍需修复皇冠裁切及坐标偏差 |
| [Boss 宽体造型](references/cloud-boss-wide-character-approved.png) | 辅助造型依据；用户认可气势，但这张棋盘下移过多，不可照搬布局 |
| [Boss 放大前布局](references/cloud-boss-layout-before-enlargement.png) | 辅助恢复棋盘/操作区关系；小 Boss 造型已淘汰 |

PNG 已复制到项目文档目录，不依赖聊天窗口或个人生成图片缓存。它们是设计参考，不是可以直接放入运行时的整屏素材，不进入 `assets` 打包。

## 接续原则

- 不重复询问已确认决定；如果要改变，说明影响并取得对应新决定。
- 已确认方向不等于数值已锁定。图上三个计数槽、血量比例、泡泡排列均不能直接当作平衡数据。
- 用户会继续讨论完整游戏；不要因为一张主界面获批就开始实现所有区域和页面。
- 就云端软糖主界面而言，下一工作是素材拆分规划、生成和一致性检查，再进入实现；也可以按用户新指示先继续其他区域/页面设计。
- 每次新确认后同步更新这些文档和参考图状态，避免只存在于聊天记录。

## 项目接入约束

Cocos Creator 3.8.8、微信小游戏、750 × 1334、Fit Width。App.scene 唯一启动，MiniGame 生命周期、GameSession、MiniGameContext、ServiceContainer、AssetService 与应用状态机遵循根 AGENTS.md。四区域属于同一小游戏内容，不是四个独立小游戏；实际区域资源组织和换景实现尚未设计，不得绕开核心加载/释放架构。

素材以静态图与代码动画为主。代码验证至少 TypeScript 无错误、git diff --check；每次改动后用 cocos_creator_stdio 刷新资源。禁止 Map/Set 迭代器展开，使用 Array.from 或显式遍历。
