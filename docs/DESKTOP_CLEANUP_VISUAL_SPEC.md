# 《桌面大清理》视觉规范 V1.0

## 1. 视觉所有权

- 主题名：**软陶微缩工作台**。
- 关键词：圆润、温暖、可触摸、轻微凌乱、整理后的清爽感。
- 所有游戏内图片归 `game-catch` Bundle；大厅图标与封面是独立生成的大厅展示副本。
- 禁止复用大厅、合成大胖橘、霓虹 2048、滑块拼图或棋类游戏的正式色板、按钮、图标、背景、特效或音频。
- 禁止出现鹅、锅、参考游戏标题/标志、可识别品牌包装、真实商标、参考游戏界面布局或相同物件组合。

## 2. 色彩与材质

| 语义 | 色值 | 用途 |
|---|---:|---|
| 墨蓝 | `#273045` | 正文、轮廓、深色槽位 |
| 暖奶油 | `#F8E8C7` | 工作垫、弹窗、留白 |
| 珊瑚 | `#EB7764` | 主行动、失败警示、铅笔/键帽 |
| 青绿 | `#56B1A6` | 帮助、继续、便签/U 盘 |
| 芥末黄 | `#E8B445` | 幸运徽章、纪录、面板描边 |
| 淡紫 | `#9B7EB0` | 重排道具、解压球 |

物件表现为哑光软陶与少量半透明树脂，边缘厚实圆润，无锐利金属高光。统一采用俯视 3/4 角、左上方大面积柔光、右下短软阴影。缩小到约 80–120 设计像素时仍须依靠轮廓与主色辨认。

## 3. 构图与 UI

- 设计基准 750×1334，Canvas 使用 Fit Width；横向占满可见宽度，纵向避让微信胶囊、顶部安全区和底部安全区。
- 全屏背景与玩法桌板必须拆分。背景只包含深胡桃色软陶工作台及边缘环境装饰，按 cover 等比铺满，禁止烘焙奶油色桌板、物件堆或 UI；奶油色桌板使用独立 1:1 正方形透明纹理，并与物件堆共同挂在 `PlayAreaRoot` 下，以同一位置和同一比例缩放。
- 顶部为确认效果图派生的正式纹理：物件组合徽标、无文字帮助按钮、无文字暂停按钮和带双层边沿、铆钉及软陶阴影的倒计时底板。倒计时数字可以动态渲染，但字形、颜色和间距必须与确认效果图一致；常驻 HUD 不展示得分或清理进度，禁止再以扁平 `Graphics` 近似正式皮肤。
- 中部玩法区始终为 1:1 正方形，正常屏幕下边长等于可见宽度并横向铺满；其垂直位置取顶部 HUD 底边与底部收纳槽顶边之间的居中位置。若上下安全边界之间无法容纳完整正方形，则对整个 `PlayAreaRoot`（桌板、物件堆和局部特效）等比缩小并水平居中，禁止裁切、拉伸桌板或让物件与桌板分别适配。162 件大尺寸物品以不规则、非对称的散乱方式分布在桌板约 90%–100% 宽度的扩展区域内，允许边缘轻微越过桌板内缘，组成二十七个交错的遮挡层；每层 6 件、相邻层保持明显压叠，禁止环形锚点、规则花瓣形、规则网格、横纵队列或多个独立小堆。运行时物品显示尺寸在原有基础上小幅上调，仍须保证真实主体多边形区域可点。
- 底部从上到下依次为七格收纳槽和三个图标化道具卡。七格槽体、三张软陶卡片底板、三个道具图标、右上问号徽章和右下次数徽章都必须使用由确认效果图派生并经透明边缘整理的本游戏正式纹理；不得以纯色圆角矩形、程序描边或通用矢量图标代替。默认界面不常驻教学提示文字，只有受阻、道具结果等必要反馈可使用短时浮层。
- 每张道具卡右上角有独立的无文字问号图标，点击后弹出当前道具的名称、效果、使用条件和广告补充规则；点击问号不得误触道具。
- 矮屏优先缩小中部正方形玩法区，仍不足时再等比缩小完整 HUD/底部控件组合；不得裁切槽位、道具或 HUD。高屏保留自然桌面留白。
- 规则、暂停、失败和结算面板采用奶油色厚软陶卡片、芥末黄描边及深色遮罩；按钮为高饱和软陶胶囊，不使用其他游戏的弹窗皮肤。

### 3.1 图标与物件边界

- 顶部帮助、暂停、标题徽标和三个道具符号全部使用本游戏正式透明纹理，不用 `Label`、Unicode 字符、字体字形或运行时临摹的扁平矢量图标冒充。
- 物品 SpriteFrame 必须按每格 Alpha 有效范围裁边，显示尺寸保持裁剪后图片原始宽高比；不再为文具追加统一灰色圆角底板或灰色描边。过长、过细、带大面积透明三角区或不规则细小突出部的文具必须重制为短粗、圆润、主体明确的版本后才能进入候选构建。
- 点击不得使用裁边矩形、椭圆、圆角矩形或所有物品共用的通用热区。每种物品必须维护与最终 SpriteFrame 对齐的归一化外轮廓多边形；触摸按下或移动时把触点逆变换到节点局部 UV，按实际显示顺序从上到下解析，并在桌板内独立的高层反馈节点上给当前候选物品加半透明遮罩和轮廓，抬起时以最终触点重新执行点在多边形内判断。点到上层实体主体就收上层，点到下层真实露出的主体就收下层，透明区域、投影和完全被覆盖的部分不得截获输入。

## 4. 正式图片资产

### 游戏 Bundle

- 当前 `desktop-cleanup-desk-v1.jpg` 因将奶油色桌板烘焙进全屏背景，只能作为开发参考，不得进入候选构建。正式版本必须提供“无桌板的胡桃木全屏背景”和“独立 1:1 奶油色桌板”两张资源。
- 顶部 HUD、七格收纳槽和三张道具卡所需的徽标、按钮、倒计时底板、槽体、卡片底板、道具图标、问号徽章及次数徽章，必须作为 `game-catch` 自有正式透明纹理随 Bundle 管理；确认效果图只用于派生与验收，不得将整张界面截图直接切片充当运行时 UI。
- `desktop-cleanup-items-atlas-v2.png` 是当前正式物件图集和旧物件冻结基线；历史 v1 图集及已删除的旧组件源目录都不是后续重建的必要输入。新增物件必须提供独立透明源图，登记到 `tools/build-desktop-cleanup-v2-assets.py` 的 `NEW_ITEM_SOURCES` 后再进入图集，并同时维护与最终 SpriteFrame 一致的归一化多边形命中形状。图集仍按固定单元管理，物件不得跨格，空白格必须保持全透明。图集更新后可运行 `python3 tools/generate-desktop-cleanup-hit-polygons.py`，按 Alpha 阈值离线重建 `DesktopCleanupGame.ts` 的生成区间。
- 三消必须提供本游戏独立的透明烟雾序列帧纹理；烟雾以暖奶油色软陶云团为主，可带少量珊瑚、青绿和芥末色颗粒，不得复用其他游戏特效。

### 大厅 Bundle

- `desktop-cleanup-icon-v1.jpg`：1:1 图标，幸运徽章、便签和铅笔组成单一清晰剪影，不含文字。
- `desktop-cleanup-cover-v1.jpg`：大厅横向卡片，软陶工作台上正在被整理的物件堆，留出安全裁切区，不直接复用游戏背景。

所有生成结果必须记录完整提示词、生成方式、日期和输出标识；透明图集需检查真实 Alpha、格线对齐、物件不跨格和无假文字。

## 5. 动效与反馈

- 点选：物件沿直线在约 200ms 内直接飞向其实际槽位，抵达后槽位轻微回弹；不加入原地弹跳、停顿或额外弧线阶段，也不允许原地缩小淡出冒充入槽动画。
- 三消：第三件物品必须先完整飞入并占据实际槽位，三件同类在槽内至少呈现一个稳定帧；随后三件以各自当前位置向三者中心汇聚，汇聚完成后播放透明烟雾序列帧并在烟雾遮盖下消失，烟雾开始后才提交槽位移除、后续物品前移、得分与通关判定。禁止第三件落槽前或落槽同帧直接删除三件。
- 被压物件：颠锅作用从最前活动层向后逐层衰减，被覆盖越深越接近静止；露出物件只做短促抬升和挪动并快速停住，不出现整堆同速滑行，也不播放翻转或翻开脉冲。
- 剩余 30 秒：倒计时转珊瑚色；禁止全屏频闪。
- 胜利：幸运徽章与芥末色短粒子庆祝；失败仅使用轻微面板震动与色彩提示。

### 5.1 已确认界面参考

- 2026-08-24 确认的第二版效果图作为本轮布局、比例和 HUD 层级的视觉基准，仅用于设计对照，不作为运行时纹理加载。
- 实现必须提取其结构语义：紧凑深层物件堆、显眼奶油色倒计时牌、顶部图片徽标与图形按钮、七格奶油色凹槽托盘、底部三个独立奶油色图标道具卡；不得把整张效果图切片后伪装成交互界面。

## 6. 独立声音方向

- 声音主题：**桌面材料小乐队**。以软陶轻弹、木桌指敲、软木与树脂短触感为核心，整体干燥、圆润、近距离，强化“拿起—归位—桌面变清爽”的满足感。
- 游戏音乐以低密度木质敲击、柔和短音槌和极轻的软陶节拍构成，不使用抢眼主旋律；必须给高频点选、三消与警告反馈留出空间。
- 高频操作反馈保持短促，准备 3 个拾取微变体，禁止长混响、尖锐高频或夸张低频；失败不使用惊吓音，胜利也不采用大型管弦庆典。
- 禁止复用或仿制大厅纸张/画廊声音、合成大胖橘的水果与猫叫、2048 的霓虹电子脉冲，以及其他游戏的正式音频、旋律和文件。
- 首个开发版本保持静音，只接入反馈事件名；正式音频必须按 `docs/DESKTOP_CLEANUP_AUDIO_PROMPTS.md` 手工生成、记录来源、验收并导入后，才允许将 Manifest 可见性从 `development` 提升。
