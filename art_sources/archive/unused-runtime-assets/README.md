# 未使用运行素材归档

归档日期：2026-08-12

此目录按原 `assets/` 层级保存已退出运行引用的图片及其 Cocos `.meta` 文件，便于恢复原路径和 UUID。

- 图片数量：213 张
- 原始体积：约 38.44 MiB
- 内容：C1 单猫、C2–C5 动画版本、C6 未选用的第三张待机候选帧、旧水果、旧大厅与旧游戏背景/封面
- 未归档：生成源和版权留档仍保留在既有 `art_sources/`、`docs/asset-generation/sources/`

当前运行素材仅保留：暖色大厅背景、中文 Logo、猫咪游戏封面、猫咪房间背景，以及 11 级猫咪各两张待机帧和一张下落帧。

如需恢复，将目标文件连同 `.meta` 移回本目录下记录的原始 `assets/...` 相对路径，再刷新 Cocos 资源数据库。
