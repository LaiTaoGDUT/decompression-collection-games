# AGENTS.md

本文件是仓库内所有编码代理的执行规范。完整依据见 `docs/ARCHITECTURE.md`、`docs/IMPLEMENTATION_PLAN.md`、`docs/CONTENT_PRODUCTION_PLAN.md`、`docs/VISUAL_STYLE_GUIDE.md` 与 `docs/PLAYTEST_ACCEPTANCE_TEMPLATE.md`；若规则冲突，以用户当前要求和 `docs/ARCHITECTURE.md` 的已确认架构基线为准。

## Codex Agent 模型与协作约定

- 遇到架构边界、跨 Bundle 依赖、生命周期与资源释放、存档迁移、并发状态、安全风险、复杂 UI/平台适配或难以解释的验证失败时，主 Agent 应将边界清晰的专项任务委派给 `sol`（昵称 `Sol`）专家 subAgent，定义见 `.codex/agents/sol.toml`。
- 委派给 Sol 的任务必须包含目标、已有证据和期望产出；Sol 只处理明确范围，不得擅自扩大需求。主 Agent 负责整合结论、采纳、执行最终修改并完成验证。
- 简单的小修改任务、确定性任务、单点文案、格式或局部确定性修改不必调用 Sol。

## 项目基线

- 引擎固定为 Cocos Creator 3.8.8，目标平台为微信小游戏；浏览器预览只作为开发和自动化验证环境。
- 游戏采用单手竖屏，设计基准为 750 × 1334；所有正式界面必须在运行时适配安全区、微信右上角胶囊、常见长短屏和不同像素密度。
- `assets/app/App.scene` 是唯一启动场景。`PersistentRoot`、公共服务和加载层由应用启动流程初始化，任何大厅或小游戏不得重复初始化全局服务。

## 架构边界

- 大厅只依赖 `GameManifest`/游戏清单，不得导入具体小游戏的类、脚本或游戏内资源。
- 每个小游戏必须具有独立场景、独立 Asset Bundle 和独立微信分包，并实现统一 `MiniGame` 生命周期。新增游戏应通过 Manifest 注册，不得为单个游戏改写核心加载流程。
- 小游戏只能通过 `MiniGameContext` 和公共服务请求暂停、重开、退出、反馈、存档、广告与统计；不得直接调用 `wx.*`、切换场景、卸载 Bundle 或自行定义全局服务。
- `wx.*` 只能出现在 `WeChatPlatform` 或明确的平台服务实现中。所有服务通过 `ServiceContainer` 获取，禁止在业务代码中散落全局单例。
- 场景切换、前后台切换、广告暂停/恢复和游戏退出必须经过应用状态机。快速重复操作不得创建重复 Session 或触发重复结算。
- 公共模块不得反向依赖具体小游戏；游戏 Bundle 之间禁止交叉引用。动态资源加载、引用追踪和释放统一经过 `AssetService`。

## 生命周期与资源释放

- 进入游戏遵循：锁定入口 → 创建 `GameSession` → 加载 Bundle/场景 → 定位入口组件 → 注入 Context → `initialize()` → `begin()`。
- 退出游戏遵循：`pause()` → 固化结果 → 保存/上报 → `dispose()` → 返回大厅 → 释放场景依赖和 Bundle → 泄漏检查。
- `dispose()` 后不得残留计时器、全局监听、微信回调、已销毁节点引用、音频实例或 Bundle 资源引用。3D 游戏还必须释放物理回调、粒子、RenderTexture、动态 Mesh、材质实例、后处理、Shader 预热与 GPU 纹理资源。
- 加载失败、初始化超时、入口缺失或单个游戏异常必须可重试或安全返回大厅，不能让整个合集失去响应。

## 屏幕适配与相机（强制）

- 每个含 `cc.Canvas` 的游戏场景都必须通过 `Canvas._cameraComponent` 显式绑定有效的 UI `cc.Camera`，字段不得为 `null`。场景里“存在 Camera 节点”不等于 Canvas 已绑定该相机。
- UI Camera 使用正交投影，只渲染当前游戏 UI Layer；Canvas、UI Camera 和 UI 根节点必须使用一致的 Layer。系统 UI、游戏 UI 和 3D 世界的 Layer 必须隔离，业务代码不得硬编码 Layer ID。
- 所有 UI 布局都在绑定后的 Canvas 坐标系中计算。使用平台安全区、微信胶囊信息和 `view.getVisibleSize()`，禁止把物理屏幕像素直接当成 UI 节点坐标。
- 除明确允许 cover 裁切的全屏背景/蒙层外，所有可见和可交互元素必须位于左右安全边界、顶部胶囊下方和底部安全边界之内。“占满屏幕宽度”默认指占满左右安全边界之间的宽度。
- 统一屏幕适配基线：游戏背景按 `cover` 等比铺满屏幕，允许上下或左右裁剪但禁止拉伸；游戏 UI 以 Cocos `Fit Width` 为基础并结合安全区布局；屏幕高度不足时，优先对核心玩法模块做等比缩放以完整落在安全区内，不能通过裁切或越界腾空间；高屏设备可以保留自然留白。
- 非对称安全区必须按真实安全内容中心布局，不得默认所有模块位于 `x = 0`。矮屏优先等比缩小主要玩法区域，不能靠裁切或越界腾空间；高屏可以保留自然留白。
- 若布局数值未超过 750 设计宽度但微信预览仍左右溢出，先检查 `Canvas._cameraComponent`、Camera 正交尺寸、可见 Layer 和 Canvas 的 `fitWidth`/适配策略，再调整业务布局参数。
- 新建或复制场景时，验证脚本必须断言 Canvas 的相机引用能够解析到 `cc.Camera`；微信验收至少覆盖一个窄屏、一个高屏以及带刘海/挖孔或非对称安全区的设备。

## 独立风格与视觉所有权（强制）

- 大厅和每个小游戏都是独立的视觉单元，必须分别定义并冻结自己的主题名称、设计关键词、色板、字体层级、构图、控件造型、图标、材质、动效和声音方向。新增小游戏不能默认继承大厅或已有小游戏的视觉体系。
- 不同小游戏之间必须保持完整的风格隔离。禁止跨游戏直接复用正式色板、按钮皮肤、图标、HUD、弹窗、Sprite、Prefab、背景、装饰、特效、音乐、音色或正式音效文件；也不得通过轻微改色把另一款游戏的皮肤当作新游戏皮肤。
- 每个小游戏独立拥有 HUD、暂停、失败、续玩、结算、规则页、图标、动效、音乐和音效的最终呈现。公共 `PausePresenter`、`ResultPresenter` 等只能定义行为与状态契约，正式视图应由当前小游戏在自己的 Bundle 中实现。
- `assets/shared` 只允许放真正跨游戏复用的无品牌技术底座，例如安全区计算、触摸与无障碍逻辑、状态语义、输入拦截、等待锁、错误恢复和开发回退视图；不得把它建设成所有游戏强制套用的通用正式皮肤库。
- 大厅不得加载小游戏的游戏内视觉资源，小游戏也不得引用大厅或其他游戏的视觉资源。大厅展示用游戏封面必须位于大厅 Bundle，并作为对应小游戏视觉方向派生的“大厅展示副本”登记到素材台账。
- 共享交互规则不等于共享美术。安全区、可触达性、状态完整性、性能门槛和生命周期可以统一，但具体视觉语言必须独立。
- 制作新游戏正式 UI 或素材前，先在对应的视觉规范文档中明确独立方向和禁止方向；没有完成风格定义时只能使用开发占位，不得从其他游戏复制正式素材顶替。

## UI、平台可用性与素材

- 微信胶囊属于不可覆盖区域。标题、计分、暂停、设置等常驻控件必须使用平台布局信息避让，禁止按截图或单一机型写死位置。
- 正式界面必须覆盖正常、加载、空、禁用和失败状态；覆盖层的实际内容面板也必须受安全区约束。
- 外部素材必须有可追溯来源和明确许可，并登记到素材台账，记录用途、来源、许可、修改、仓库路径和 Bundle 归属。来源未确认的素材只能作为开发占位，禁止进入候选构建；不得直接取用其他游戏或模仿可识别品牌/艺术家风格。
- 玩法参数、广告位、频控和可调整文案应配置化，不要散落魔法数。

## 存档与兼容

- 根存档和各游戏数据都必须携带版本。单个小游戏只能读写自己的数据命名空间，禁止因游戏隐藏、回滚或字段不识别而清空用户根存档。
- 存档结构变化必须提供逐级 `vN → vN+1` 迁移。迁移成功并通过当前结构校验后才能覆盖主存档；失败时先备份原始字符串，再恢复安全默认值。
- 修改核心协议、状态机、Bundle 边界、数据结构或存档语义前，先更新 `docs/ARCHITECTURE.md`，记录原因、替代方案、兼容影响、迁移和回滚方式。未记录的临时绕过不得进入正式实现。

## 实施与验证

- 修改前先定位已有实现和同类游戏的成熟做法；保留用户已有改动，不覆盖无关文件或素材。
- 每个步骤完成后先执行针对性验证和代码 Review，再继续后续工作。验证失败时修复根因，不用后续功能或硬编码掩盖问题。
- 最低完成门槛：TypeScript 无编译错误、相关游戏的构建/运行验证通过、`git diff --check` 通过。涉及 Bundle 或微信构建时，还要运行：

  ```text
  npm run verify:game-bundles
  npm run verify:wechat-package-size
  ```
- 涉及显示、输入、安全区、生命周期或平台行为的改动，不能只依赖静态检查：必须重新编译。
